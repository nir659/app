{
  "name": "app",
  "private": true,
  "scripts": {
    "dev": "live-server html --port=8080 --open=/",
    "start": "live-server html --port=8080"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
